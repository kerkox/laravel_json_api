models:
  inventory:
    description: string
    user_id: id
    relationships:
      hasMany: stockroom
  stockroom:
    description: string
    relationships:
      hasMany: item
  item:
    description: string

